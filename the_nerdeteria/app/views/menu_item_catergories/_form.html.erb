<%= form_for(@menu_item_catergory) do |f| %>
  <% if @menu_item_catergory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_item_catergory.errors.count, "error") %> prohibited this menu_item_catergory from being saved:</h2>

      <ul>
      <% @menu_item_catergory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
